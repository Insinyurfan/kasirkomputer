# Copy to ".env" (or just run `npm run dev` — it is created automatically).

# SQLite database file (relative to the prisma/ directory)
DATABASE_URL="file:./dev.db"

# Secret used to sign the session cookie. Left blank here on purpose — it is
# generated automatically the first time the app starts and written back here.
SESSION_SECRET=""
